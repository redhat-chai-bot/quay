/**
 * Entry file required by the fec (frontend-components-config) build toolchain.
 *
 * For consoledot plugin builds, the actual application entry point is provided
 * via module federation (see fec.config.js moduleFederation.exposes).
 * This file only serves as the webpack entry so that the fec build does not
 * fail looking for src/entry.{ts,tsx,js,jsx}.
 */
